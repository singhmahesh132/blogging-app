server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./blog_app;
  jpa:
    hibernate:
      ddl-auto: create #in prod always use update
    show-sql: true