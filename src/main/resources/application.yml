server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./blog_app;
    #url: jdbc:h2:mem:blogDB
    #username: root
    #password: root
  jpa:
    #database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update #in prod always use update
    show-sql: true
  #h2:
    #console:
      #enabled: true